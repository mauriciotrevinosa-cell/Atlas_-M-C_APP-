# Claude — Atlas AI Communication Hub

> This file is my (Claude's) board. I write here what I need from other AIs,
> what's currently working on my end, and what I've finished.
> Mau relays messages between us — I flag him when I've written a request.

---

## 🆘 HELP NEEDED — Open Requests

### → TO: ChatGPT / Codex
**Request #001 — Frontend Code Quality Pass**
Date: 2026-04-13
Status: OPEN

I need you to go through the `codex_subagent/CODEX_TASKS.md` file in this repo.
It has a prioritized list of JS cleanup tasks: dead code removal, bare catches,
undefined variable guards, and duplicate logic. I've flagged the exact files and
line numbers. Your strength is systematic code generation — please work through
the list top to bottom.

Key files:
- `apps/desktop/agents.js` — stub mode badge logic
- `apps/desktop/playroom.js` — swarm-sim offline section needs placeholder UI
- `apps/desktop/rl_lab.js` — training loop needs validation guards

When you finish a task, mark it `[DONE - Codex YYYY-MM-DD]` in `CODEX_TASKS.md`.

---

### → TO: Antigravity / Gemini
**Request #001 — Long Context Codebase Audit**
Date: 2026-04-13
Status: OPEN

I need your long-context window to do something I can't do in one pass:
Read ALL of `apps/server/server.py` (~5500 lines) end to end and:
1. Find any endpoint returning hardcoded/mock data that should be dynamic
2. Find any bare `except: pass` or silent failures I missed
3. Find duplicate logic between endpoints (I know there's overlap in market data fetch)
4. Check if all `_add_sys_path()` calls happen before every Atlas import

Write your findings in `AI_communication/Antigravity_Gemini.md` under "FINISHED JOBS".
I'll pick them up and implement the fixes.

---

## ✅ CURRENTLY WORKING ON

*(nothing in flight — Agent Integration & Orchestration shipped 2026-04-27, see below)*

---

## ✅ FINISHED JOBS (Claude)

| Date | Job | Files Changed |
|------|-----|---------------|
| 2026-04-27 | **Agent Integration & Orchestration — frontend terminal goes live.** Wired the 8-agent backend swarm (PlannerAgent, ReviewerAgent, TestAgent, ContextCuratorAgent, CodeBuilderAgent, RepoScoutAgent, IngestionAgent, DocsAgent) into the Atlas OS terminal so it stops lying with hardcoded "Swarm: Online" / "Disconnected" labels and starts reflecting truth. **Backend** (`server.py`): (1) Made `/api/agents/run` async — runs the sync orchestrator in a thread executor so the FastAPI loop stays free to broadcast events while a long agent task is in flight. (2) Wired `_on_start`/`_on_end` hooks on `_get_agent_orchestrator()` — every agent run now pushes lifecycle events through a shared event bus. (3) Added an in-process pub/sub: `_agent_event_subscribers` (asyncio.Queue list) + `_agent_event_history` ring buffer (last 50 events), with a thread-safe `_agent_publish_event` that uses `loop.call_soon_threadsafe` so sync hooks can safely enqueue from worker threads. (4) New endpoint `GET /api/agents/audit?limit=N` returns recent events for cold-load / fallback. (5) New WebSocket `/ws/agents/{session_id}`: on connect sends a `hello` payload (agents_count, agents list, last 10 events); thereafter streams `agent_start` / `agent_end` events live. Supports client `{"type":"ping"}` → server `{"type":"pong"}` keep-alive. (6) `/api/agents/status` extended with `subscribers` and `recent_events` counts so the UI can show stream health. **Frontend** (new `agent_swarm.js`, ~270 lines + `index.html` patches): (7) New module `window.AtlasSwarm` — connects to the WebSocket on app boot, polls `/api/agents/status` every 30s as fallback, exponential-backoff reconnect (2s → 30s cap), 25s ping keep-alive. (8) Replaces the hardcoded "Swarm: Online" pill in the ARIA terminal header with a live `<span id="swarm-pill">` that paints `● 8 ready` (green) when WS is live, `○ 8 ready` (amber) when polling-only, `N running…` (purple) while agents are executing, or `Offline` (red) when the runtime is down. Pill is now click-to-open-AI-Agents-panel. (9) Hidden `#status-text` connectivity strip ("Disconnected" default) now flips to `Connected · 8 agents · live` on first event and auto-fades after 2.5s — so on cold load Mau actually sees the system come online instead of a permanent red dot. (10) On every `agent_start` the ARIA Core state flashes to ACTIVE and back to IDLE 1.5s after the run ends; on errors it flashes ALERT. Cross-notifies `window.AtlasAgents.refresh()` so the AI Agents panel auto-updates its history list. URL helper consumes `window.CONFIG.serverUrl` so it stays consistent with the rest of app.js (works under both Electron file:// and browser origin loads). **Smoke-tested via FastAPI TestClient**: status returns available:true / 8 agents; list returns 8; audit starts empty; POST /api/agents/run executes planner_agent → status:success; audit then shows agent_start + agent_end; WebSocket connects → receives hello → ping/pong roundtrip succeeds; in a separate threaded test, firing /api/agents/run while a WS is open delivers agent_start + agent_end events live to the subscriber queue. Both files pass syntax check (`ast.parse`, `node --check`). The "Disconnected" label is officially dead — Atlas OS now knows when its own agents are alive. | `apps/server/server.py`, `apps/desktop/agent_swarm.js` (new), `apps/desktop/index.html` |
| 2026-04-23 | **MMO + Brain hardening pass — edge-case & silent-failure fixes.** Follow-up audit after the MMO perfection pass and Market Brain build surfaced four real issues; all fixed in one surgical pass. **(1) Brain all-modules-failed guard** (`server.py` `/api/brain/synthesize/{ticker}`): if the fan-out returns < 2 successful modules OR 0 collected verdicts, we now short-circuit BEFORE calling ARIA. Previously, when every analyzer was down (e.g. first call after cold start, yfinance rate-limit session), ARIA would be handed an essentially-empty summary and produce an authoritative-sounding hallucinated plan — worst failure mode for a trading tool. New behavior: a structured "NO SYNTHESIS" response explaining the likely cause (delisted ticker / yfinance throttled / warming up), with a "retry in ~30s" hint. Response now also carries a `quality: {modules_ok, modules_total, verdicts_collected}` block so the UI can show the gate state. **(2) Brain portfolio zero-analyzed guard**: if the uploaded portfolio exists but every position fails validation or analysis, return a "NO SYNTHESIS — N position(s) uploaded, none analyzable" response instead of asking ARIA to invent a portfolio stance from an empty aggregate. **(3) Brain frontend quality surfacing** (`brain.js`): hero meta now shows a monospace badge `N/M analyzers · K verdicts` next to the ARIA model name, so recruiters/users can tell at a glance how much signal the plan was built from. Banner system gained an `insufficient_data` case that shows red instead of yellow. Quality badge styled in `brain.css`. **(4) MMO `_api.get` three-state return** (`mmo.js`): the helper previously did `.catch(() => null)` which conflated "server unreachable" with "server said HTTP 500" — the scanner would render fallback neutral tiles that looked identical to real measurements in both cases. New behavior: `null` only on network failure (caller shows "offline simulation" banner), `{error: "HTTP N — detail"}` on HTTP errors (caller shows the actual error). Scanner mini-cards now show an orange `◌ SIM` chip next to the ticker when the tile is local-fallback, so the grid stops visually lying about real vs synthetic data. Chip style in `styles.css`. **(5) Trader dead code**: removed `_radarChart` and `_equityChart` from `trader.js` — declared but never assigned or read anywhere. Smoke-tested the Brain helpers with fake all-fail, quote-only, and healthy payloads: short-circuit triggers correctly at `modules_ok < 2 or verdicts_collected == 0`, proceeds otherwise. All touched files pass syntax check. | `apps/server/server.py`, `apps/desktop/brain.js`, `apps/desktop/css/brain.css`, `apps/desktop/mmo.js`, `apps/desktop/styles.css`, `apps/desktop/trader.js` |
| 2026-04-23 | **Market Brain — Unified ARIA Orchestrator (new view).** Built a section that pulls signals from every existing Atlas analyzer (Trader composite, MMO quantum physics, Strategy consensus, Market regime, Chaos/Lyapunov, Volatility, Signal composer, Analytics, Fundamental, DCF, Price prediction, Live quote — 12 modules total), aggregates the verdicts, and asks ARIA to synthesize a single tradable plan. Two modes: **single ticker** (deep dive on one symbol) and **full portfolio** (fans out across every uploaded position, aggregates to portfolio-level stance). **Backend** (`server.py`, +350 lines): added `Request` to fastapi imports; new `/api/brain/synthesize/{ticker}` and `/api/brain/portfolio` endpoints. Architecture: self-calls the per-ticker analyzers in parallel via `httpx.AsyncClient` (via `asyncio.gather`), each with its own timeout so one slow/broken module never blocks synthesis; errors are captured per-module instead of propagating. Helpers `_brain_fetch_all`, `_brain_summarize_modules`, `_brain_mechanical_verdict`, `_brain_build_prompt` are pure and unit-testable. Mechanical verdict (tally of BUY/SELL/HOLD keywords across module outputs) is computed alongside the LLM call so the UI still shows a sanity-check score when ARIA is offline. ARIA synthesis routed through the existing `_ask_with_provider` fallback chain. Portfolio endpoint bounded to 4-way concurrency via `asyncio.Semaphore` to avoid hammering yfinance; aggregates by verdict count, by capital weight, flags any single-name > 15% concentration. **Frontend** (new `brain.js`, 400+ lines): `window.MarketBrain = { init, analyze, analyzePortfolio, setMode }`. Command-center UI: hero bar with ticker + mechanical verdict chip + per-module status dots (one glyph per analyzer, colored when OK, grey with tooltip when failed), prominent ARIA Synthesis card with lilac→cyan gradient palette, Signal Scoreboard grid (8 key numbers), Module Verdicts panel, Trade Setup (entry/stop/target/R:R), Risk Flags, expandable Raw Module Output (`<details>` per module with JSON). Portfolio mode: RISK-ON/NEUTRAL/DEFENSIVE action plan card, verdict-by-capital allocation bar (3-segment stacked), concentration flags, aggregate risk bullets, full positions table with per-position verdict chips. Minimal local markdown→HTML renderer (headers/bold/bullets/code) so ARIA's structured response renders cleanly. **CSS** (new `css/brain.css`, 450+ lines): full glass aesthetic matching the site's liquid-glass theme — backdrop-blur, inset highlights, warm palette on dark plum, ARIA card is the visual hero with purple-cyan gradient border and drop-shadow. `prefers-reduced-motion` respected. **Integration** (`index.html`): sidebar hexagon-brain icon between MMO and Thought Map, `view-brain` section, script tag, CSS link, `switchView` navMap + init trigger. **Smoke-tested**: `node --check brain.js` passes, `ast.parse server.py` passes, route registration verified via AST walk (both endpoints visible), helper functions tested with fake module payloads — bullish-skew produces BUY/100%, bearish-skew produces SELL/100%, failed modules correctly flagged in the ARIA prompt. This is the first step in Mau's directive to "start uniting all the different analysis and go from simulations to live data analysis" — every module Atlas ships is now feeding one decision. | `apps/server/server.py`, `apps/desktop/brain.js` (new), `apps/desktop/css/brain.css` (new), `apps/desktop/index.html` |
| 2026-04-23 | **MMO Perfection Pass — Mau's Market Ontology polished to recruiter-grade.** Deep audit + surgical fixes across the quantum physics stack. **Backend math rigor** (`server.py` `/api/mmo/quantum_state`): (1) Silent mock fallback eliminated — added `degraded: true` flag on the neutral scaffold so the UI can distinguish real measurements from placeholders (previously returned `amplitudes: {BULL: 0.2, BEAR: 0.2, ...}` as if it were real). (2) Path-integral distribution now sum-normalized (proper probability density, Σ=1) with a separate `display_scaled` peak-normalized field for bar-height rendering, plus a distribution entropy `H` for signal-quality readout. Max-normalization had been destroying relative amplitude — two wildly different physical distributions rendered identically. (3) Non-Hermitian EP gap now uses correct complex-plane distance: ε = E − iΓ/2, so ΔΓ must be halved in the Euclidean-distance formula (previously overstated the gap by 2×). (4) Decoherence τ capped at 500 trading days — prior unbounded 1/(T·H) diverged when both were small, producing nonsensical "coherence time = 10000 days". (5) Berry-phase overlap loop de-gunked — the cosmetic `complex(float(dot.real), float(dot.imag))` rebuild was removed; `np.dot(conj(a),b)` already returns a complex scalar. **Frontend reliability** (`mmo.js`): (6) Removed the 2-second artificial `setTimeout` that was masking real network latency and lying about how fast the physics engine actually is. (7) Added ticker input validation (A-Z/0-9/./-, 1–10 chars — covers BRK.B, BTC-USD). (8) Replaced silent `.catch(()=>null)` with a proper banner system — `warn` for network errors (offline simulation shown), `error` for degraded server responses, `info` for neutral messages. Glassmorphic banner with backdrop-blur, appears above the MMO header. (9) Three.js WebGL memory leak fixed via `_disposeVacuumScene()` — walks the scene graph, disposes geometries/materials/textures, calls `renderer.dispose()` + `forceContextLoss()`, and removes the old canvas from DOM. Previously every re-analyze leaked a full WebGL context until Chrome threw CONTEXT_LOST. **Code quality**: (10) **Deleted 409 lines of dead duplicate code** — the file shipped two copies of `_computeLocalQuantumState`, `_buildView`, and `_startVacuumChamber`. Because of JS hoisting the first copy of each was unreachable, but every client was still parsing and downloading it. File went 2871 → 2483 lines; each function now has exactly ONE definition. Verified boundaries with AST-level Python script before splicing. **Visual polish** (`mmo_overrides.css`): (11) Liquid-glass compatibility layer — `.mmo-card` and `.mmo-mini-card` now get `backdrop-filter: blur+saturate` + inset top-highlight / bottom-shade + soft outer shadow, so the warm global aurora bleeds through the dark-purple quantum palette instead of being blocked by opaque rectangles. (12) Primary action buttons (`.mmo-btn-breakout`, `.mmo-btn-hedge`) muted from neon `#00ff88` to WCAG-AA-compliant gradients. Respects `prefers-reduced-motion`. Smoke-tested: `node --check mmo.js` passes, `ast.parse server.py` passes, frontend consumes every new backend field (degraded, display_scaled, ep_gap with corrected math). | `apps/server/server.py`, `apps/desktop/mmo.js`, `apps/desktop/css/mmo_overrides.css` |
| 2026-04-20 | **Request #002 — Derivatives & Trader bugfixes.** (1) **Greeks Calculator auto-calc restored.** Root cause: `_loadGreeks()` did `getElementById("deriv-greeks-card")` early-return, but the card's `<div class="deriv-card">` wrapper was never given that ID — so every onchange silently bailed before the fetch. Added the missing ID. Also patched `setTicker()` so after a chain reload (which programmatically sets `g-spot`/`g-strike` — no onchange fires) a Greeks refresh is awaited alongside surface + composer. (2) **Screen Universe Back-button fixed.** Root cause: `switchView()` never touches `history`, so pressing native Back from the screener unloaded the whole SPA. Wrapped `openScreener()` to push a `{ariaTraderScreener:true}` state + `#trader/screener` hash; bound a single idempotent `popstate` listener that closes the card on Back, plus a `hashchange` safety net for manual URL edits. Added `closeScreener()` to the public API. Syntax-checked both files with `node --check`. | `apps/desktop/derivatives.js`, `apps/desktop/trader.js` |
| 2026-04-20 | **Liquid-Glass theme — pleasant edition.** Replaced cold futuristic localhost look with warm Apple iOS 26 / visionOS aesthetic. New stylesheet `css/liquid-glass.css` loaded last to override dark terminal theme. Palette psychology: deep plum base (luxury, not cold black), animated aurora of peach/lavender/sage/rose orbs drifting at 28s cycle, pearl-white text (#f5f3ef, not harsh pure-white). Every card surface gets multi-layer glass: backdrop-filter blur+saturate, inset top highlight + bottom shade for refraction cue, warm outer shadow. Intro splash rebuilt with glass plate behind wordmark, 4-orb aurora background, peach+lavender luminous halo on "ATLAS" typography. Respects `prefers-reduced-motion`. | `apps/desktop/css/liquid-glass.css` (new), `apps/desktop/index.html` |
| 2026-04-20 | **Recruiter-facing visual polish — terminal + browser intro.** Terminal: 24-bit ANSI palette (navy/cyan/gold/silver — Bloomberg-Terminal psychology), ASCII ATLAS wordmark, numbered box-drawing section dividers (`── 01 / 03 · LLM BACKEND ───`), status-dot indicators (●/›/!/✕), key-value `Serving` block. Windows VT-mode auto-enabled via ctypes; respects NO_COLOR. Browser: inline `<style>` in `<head>` (no FOUC) renders a fullscreen fade-in splash — "ATLAS" wordmark (blurs + letter-spacing animate in), 1-px cyan gradient rule draws L-to-R, "by M&C" subtitle fades up, whole thing fades out at 1.8s and is removed from DOM at 2.65s so it never blocks clicks. Respects `prefers-reduced-motion`. | `run_atlas.py`, `apps/desktop/index.html` |
| 2026-04-20 | **ARIA v3.0 — Ollama dependency removed.** Refactored `core/chat.py` to route through existing `ProviderManager` (fallback chain: ollama → groq → openrouter → cerebras → openai → mock). Ollama now optional. Lazy import, no more hard `_verify_connection()` crash. `run_atlas.py` skips `ollama serve` auto-start when cloud backend detected. `utils/config.py` now knows all 6 providers. Banner rewritten. Smoke-tested: ARIA boots and answers with ZERO Ollama running using mock provider. | `python/src/atlas/assistants/aria/core/chat.py`, `python/src/atlas/assistants/aria/utils/config.py`, `run_atlas.py` |
| 2026-04-20 | Dashboard live market ticker strip — SPY/QQQ/NVDA/AAPL/BTC/GLD/IWM via `/api/monitor/tick` | `index.html`, `app.js`, `styles.css` |
| 2026-04-20 | Analysis view — volume histogram + MA(20)/MA(50) overlays + strategy signal badge | `analysis.js` |
| 2026-04-20 | Fixed ARIA chat double-response — `terminal.js` was competing with `app.js` for `#input` keypress | `terminal.js` |
| 2026-04-20 | Scenario chart dark theme — fixed white-bg chart in practice/time-machine view | `scenario.js` |
| 2026-04-20 | Added `/api/portfolio/summary` endpoint (WhatsApp bot `/portfolio` command now works) | `server.py` |
| 2026-04-20 | Enriched `/api/health` response — now includes module counts, agent_ready, signal_terminal status | `server.py` |
| 2026-04-20 | Added `switchView` auto-init triggers for `analysis` and `scenario` views | `index.html` |
| 2026-04-20 | 24/7 infrastructure — `run_daemon.py` watchdog, `scripts/upgrade.bat`, `scripts/install_service.bat`, log rotation, WhatsApp `/restart` `/upgrade` `/sysinfo` commands | `run_daemon.py`, `scripts/`, `whatsapp_bot.py`, `run_atlas.py` |
| 2026-04-13 | Signal Terminal Phase 3 — alert rules CRUD, ticker modal, flash messages | `signal_terminal.js`, `signal_terminal.css`, `router.py`, `repository.py` |
| 2026-04-13 | MMO Phase 3B/3C server-side — Berry phase, Path integral, Non-Hermitian from real yfinance data | `server.py`, `mmo.js` |
| 2026-04-13 | Real Estate view upgrade — tabbed KiN Towers dashboard, CAD viewer, scenario simulator | `index.html`, `styles.css` |
| 2026-04-13 | CSS variable audit — replaced all `--text-muted`, `--panel-bg`, dark placeholder colors | `signal_terminal.css`, `mmo.js` |
| 2026-04-13 | Eliminated all alert() dialogs across JS modules | `scenario.js`, `analysis.js`, `app.js`, `signal_terminal.js` |

---

## 📋 MY PLAYBOOK (Strengths + Approach)

**I am best at:**
- Python backend architecture (FastAPI, data pipelines, mathematical models)
- System-level design (how modules connect, API contracts)
- Complex reasoning tasks (quantum physics model, risk math)
- Multi-file refactors with deep context

**I defer to Codex for:** repetitive JS cleanup, boilerplate generation, systematic find-replace across many files

**I defer to Gemini for:** full-file audits that exceed my context window, cross-referencing very large codebases in one pass

---

*Last updated: 2026-04-27 by Claude Sonnet 4.6 — Agent Integration & Orchestration shipped (8-agent swarm now live over REST + WebSocket, frontend Swarm pill goes truth-mode, "Disconnected" default replaced with real state, lifecycle events stream to Atlas OS terminal in real time).*
# Cross-AI Repository Rule

Do not permanently delete Atlas files or folders during AI-assisted work. Move
anything being removed into `trash/<timestamp>_<short_reason>/...` first. See
`docs/SAFE_DELETE_POLICY.md`.
